<%= form_for(@votacion) do |f| %>
  <% if @votacion.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@votacion.errors.count, "error") %> prohibited this votacion from being saved:</h2>

      <ul>
      <% @votacion.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :proyecto_id %><br>
    <%= f.number_field :proyecto_id %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.number_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :valor %><br>
    <%= f.number_field :valor %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
